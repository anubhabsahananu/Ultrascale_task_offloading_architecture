By integrating the compiled bitstream and hardware handoff files into the PYNQ environment on the ZCU104, you can launch the server.py middleware to fully replicate this multi-tenant hardware-accelerated architecture. This setup allows the Flask-based server to interface directly with the custom IP cores, enabling the same resource-aware scheduling and deterministic execution results reported in the study.
